@model IEnumerable<jxgl.Models.Teacher>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Tname)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Tsex)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Tacad)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Tpwd)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Tname)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Tsex)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Tacad)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Tpwd)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Tno }) |
            @Html.ActionLink("Details", "Details", new { id=item.Tno }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Tno })
        </td>
    </tr>
}

</table>
