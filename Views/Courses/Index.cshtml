@model IEnumerable<jxgl.Models.Course>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Cname)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Ctime)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Ctrem)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Ccredit)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Cname)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Ctime)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Ctrem)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Ccredit)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Cno }) |
            @Html.ActionLink("Details", "Details", new { id=item.Cno }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Cno })
        </td>
    </tr>
}

</table>
